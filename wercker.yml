box: wercker/default
build:
  steps:
    - arjen/hugo-build:
        version: 0.14
        theme: hyde
        flags: --disableSitemap=true
deploy:
  steps:
    - lukevivier/gh-pages@0.2.1:
        token: $GIT_TOKEN
        domain: www.kocka-gene.io
        basedir: public