box: debian
build:
  steps:
    - arjen/hugo-build:
        version: "0.14"
        theme: redlounge
        config: config.toml
        flags: --disableSitemap=true
        deploy:
  steps:
    - gh-pages:
        token: $GIT_TOKEN
        domain: www.kocka-gene.io
        basedir: build