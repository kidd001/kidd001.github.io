box: wercker/default
build:
  steps:
    - arjen/hugo-build:
        theme: hyde
        config: config.toml
        flags: --disableSitemap=true
deploy:
  steps:
    - lukevivier/gh-pages:
        token: $GIT_TOKEN
        repo: kidd001/kidd001.github.io
        domain: www.kocka-gene.io
        basedir: public